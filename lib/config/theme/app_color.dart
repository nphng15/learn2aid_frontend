import 'package:flutter/material.dart';

class AppColors {
  const AppColors();

  // Primary and Secondary Colors
  static const Color primary = Color(0xFF215273);     // Màu xanh đậm
  static const Color secondary = Color(0xff55c595);   // Màu xanh lá

  // Background Colors
  static const Color backgroundBlue = Color(0xFF71C5FF); // Màu xanh nền Login
  static const Color backgroundLight = Color(0xFFFFFFFF); // Trắng
  static const Color backgroundDark = Color(0xFF000000);  // Đen
  static const Color white = Color(0xFFFFFFFF);           // Trắng
  static const Color black = Color(0xFF000000);           // đen

  // Greyscale Colors
  static const Color defaultGrey = Color(0xFFEEEEEE); // Xám nhạt
  static const Color grey2 = Color(0xFFF0F0F0);       // Xám nhạt hơn
  static const Color grey3 = Color(0xFFB1B1B1);       // Xám trung bình
  static const Color grey4 = Color(0xFFD0D0D0);       // Xám đậm hơn

  // Error and Alert Colors
  static const Color error = Color(0xFFF24141); // Đỏ cảnh báo

  // Button Colors
  static const Color buttonPrimary = Color(0xff55c595);   // Nút chính
  static const Color buttonDisabled = Color(0xFFB1B1B1);  // Nút bị vô hiệu
}
